info:
  name: update a buddy
  type: http
  seq: 5

http:
  method: PUT
  url: "{{BASE_URL}}/buddies/:id"
  params:
    - name: id
      value: EMP1776587528774
      type: path
  body:
    type: json
    data: |-
      {
        "nickName": "silentFellow"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
