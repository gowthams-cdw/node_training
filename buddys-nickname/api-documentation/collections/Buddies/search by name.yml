info:
  name: search by name
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/buddies/search?name=gowtham s"
  params:
    - name: name
      value: gowtham s
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
