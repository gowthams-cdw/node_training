info:
  name: get all buddies
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/buddies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
