info:
  name: search by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/buddies/search?id=neo"
  params:
    - name: id
      value: neo
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
