info:
  name: create a user
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/users/"
  body:
    type: json
    data: |-
      {
        "username": "silentFellow",
        "password": "silentFellow"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
