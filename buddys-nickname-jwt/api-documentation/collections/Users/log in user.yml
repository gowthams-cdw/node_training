info:
  name: log in user
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/users/login"
  body:
    type: json
    data: |-
      {
        "username": "silentFellow",
        "password": "silentFellow"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
