info:
  name: delete the current user
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/users/delete"
  headers:
    - name: Authorization
      value: Bearer {{JWT_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
